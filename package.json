{
  "name": "action-check-certificate",
  "version": "1.0.0",
  "description": "",
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc --noEmit && esbuild --bundle src/index.ts --platform=node --outfile=lib/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "1.10.1"
  },
  "devDependencies": {
    "@types/node": "20.11.26",
    "esbuild": "0.20.1",
    "typescript": "5.4.2"
  }
}
